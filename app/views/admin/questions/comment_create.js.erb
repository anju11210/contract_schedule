<!--NOTE:id="comments_areaをrenderで指定した部分テンプレートに更新-->
$("#comments-area").html("<%= j(render 'admin/questions/comment_index', question: @question) %>")
<!--NOTE:送信後のフォーム内を空にする-->
$("#comment_body").val("")
<!--NOTE:errorが発生しているcommentのままだとエラーメッセージが消えないため、create後は発生していないものにここで更新-->
$("#comments-error").html("<%= j(render 'layouts/error_messages', model: @comment) %>");
<!--NOTE:フラッシュメッセージ-->
$('#flash-message').html("<%= j(render 'layouts/flash') %>");

